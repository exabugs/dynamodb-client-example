{
  "name": "@ainews/maintenance-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "Maintenance Worker Lambda - DynamoDBレコードを走査してシャドーを修復するワーカー",
  "main": "dist/handler.mjs",
  "scripts": {
    "build": "node esbuild.config.js",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit && eslint src --ext ts --max-warnings 0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@exabugs/dynamodb-client": "^0.1.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/util-dynamodb": "^3.700.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
