{
  "name": "@ainews/maintenance-coordinator",
  "version": "1.0.0",
  "type": "module",
  "description": "Maintenance Coordinator Lambda - シャドー整合性メンテナンスを開始するオーケストレーター",
  "main": "dist/handler.mjs",
  "scripts": {
    "build": "node esbuild.config.js",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit && eslint src --ext ts --max-warnings 0",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-sfn": "^3.700.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^22.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "esbuild": "^0.24.0",
    "eslint": "^9.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
