# Environment variables example
# 
# This file is a template. Actual environment files are auto-generated by:
#   make env-admin ENV=dev   # generates .env.development
#   make env-admin ENV=stg   # generates .env.staging
#   make env-admin ENV=prd   # generates .env.production
#
# Vite automatically loads the appropriate file based on the mode:
#   vite dev                 # uses .env.development
#   vite build --mode staging # uses .env.staging
#   vite build               # uses .env.production

# Records Lambda Function URL
VITE_RECORDS_API_URL=https://xxxxx.lambda-url.us-east-1.on.aws/

# Cognito User Pool設定
VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
VITE_COGNITO_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_DOMAIN=example-dev-auth
VITE_COGNITO_REGION=us-east-1
